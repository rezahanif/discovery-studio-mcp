{
 "_doc": "Authored intent phrasings. The AiConnect gateway folds these into its BM25 search haystack only - never into what the model is shown, and never into the embedding.",
 "_rule": "An alias must not introduce a word that is COMMON in ordinary English but RARE in the corpus. BM25 IDF treats such a word as maximally discriminating, so it becomes a magnet for queries the tool should refuse. Name the OPERATION - not a material, a trade, or any everyday noun. Authored from each tool's own name and description only, never from the benchmark task prompts.",
 "_why": "Driver: aiconnector/scripts/bundle-benchmark/alias_ab.py (BUNDLE_BENCH_TARGET=discovery).",
 "aliases": {
  "ds_get_capabilities": [
   "what this discovery studio install supports",
   "available adapters and licence status",
   "is pipeline pilot reachable"
  ],
  "ds_health_check": [
   "verify the installation and dependencies",
   "perl and pipeline pilot readiness",
   "component safety check"
  ],
  "ds_inspect_structure": [
   "chains residues and atoms in a structure file",
   "ligands waters and heteroatoms present",
   "pdb file contents"
  ],
  "ds_validate_structure": [
   "is this structure suitable for a workflow",
   "check a molecular file before running"
  ],
  "ds_convert_structure": [
   "pdb to mol2",
   "change molecular file format",
   "sdf and mol2 interconversion"
  ],
  "ds_list_protocols": [
   "which protocols are available",
   "protocol catalogue"
  ],
  "ds_describe_protocol": [
   "protocol parameters and defaults",
   "required inputs of a protocol",
   "licence requirements of a protocol"
  ],
  "ds_run_protocol": [
   "execute a docking protocol",
   "launch a minimisation or simulation protocol",
   "submit a pipeline pilot run"
  ],
  "ds_get_job_status": [
   "has the submitted run finished",
   "progress of a job"
  ],
  "ds_cancel_job": [
   "abort a submitted run",
   "terminate a running job"
  ],
  "ds_list_jobs": [
   "recent and active jobs",
   "job queue listing"
  ],
  "ds_render_structure": [
   "render a molecular image",
   "visualise the protein in the gui session"
  ],
  "ds_search_api": [
   "find a scripting api method by keyword",
   "perl api reference lookup",
   "which api method covers this"
  ],
  "ds_function_registry": [
   "documentation for a named api function",
   "parameters and usage of an api function"
  ],
  "ds_list_api_categories": [
   "browse api packages",
   "api surface by domain"
  ]
 }
}
